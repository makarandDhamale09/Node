// const { generateRandom, celciusToFahrenheit } = require("./utils");

// console.log(generateRandom());

// console.log(`Celcius to fahrenheit : ${celciusToFahrenheit(1)}`);

import { getPosts } from "./postController.js";

console.log(getPosts());
